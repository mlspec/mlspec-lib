# BASE_64 ENCODED STRING THAT MUST CONTAIN THE FOLLOWING IN YAML FORMAT

# url: "wss://ACCOUNT_NAME.gremlin.cosmos.azure.com:443/"
# key: "KEY"
# database_name: "DATABASE_NAME"
# container_name: "COLLECTION_NAME"

# Use the following:

# import base64
# c = """
# url: "wss://ACCOUNT_NAME.gremlin.cosmos.azure.com:443/"
# key: "KEY"
# database_name: "DATABASE_NAME"
# container_name: "COLLECTION_NAME"
# """
# base64_encode = base64.urlsafe_b64encode(string.encode("utf-8"))
# final_encode_to_utf8 = str(base64_encode, "utf-8")
#
