mlspec_schema_version:
  # Identifies the version of this schema
  meta: 0.5.0

mlspec_schema_type:
  # Identifies the type of this schema
  meta: dlrm

schema_version:
  # User previded: Identifies version of dlrm to use
  type: semver
  required: True

schema_type:
  # User previded: Identifies that this is a dlrm benchmark
  type: string
  required: True

global_batch_size:
  description: 'global batch size'
  type: int
  required: True

opt_base_learning_rate:
  description: 'base learning rate, this should be the learning rate after warm up and before decay'
  type: float
  required: True
